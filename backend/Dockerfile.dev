# ============================================
# Development Dockerfile - Backend
# Avec hot reload (tsx watch)
# ============================================
FROM node:22-alpine

WORKDIR /app

# Installer les dépendances
COPY package*.json ./
RUN npm install

# Copier tsconfig
COPY tsconfig.json ./

# Le reste est monté via volumes dans docker-compose.dev.yml

# Exposer le port
EXPOSE 3000

# Par défaut, mais sera overridé par docker-compose
CMD ["npm", "run", "dev"]
